name: Docker Security
on:
  push:
    branches: [main]
  schedule:
    - cron: '59 5 * * 4'
  workflow_dispatch:
permissions: {}
jobs:
  docker-security:
    permissions:
      contents: read
      security-events: write
    uses: rknightion/.github/.github/workflows/docker-security.yml@873962525bd50b84dfdb3226efab8c8e2cd6a9a2 # v1.11.0
